
% \begin{proof}
%   Because $B$ separates $X$ with the pair $(U, V)$ $X\setminus B$ is equal to the disjoint union of $U$ and $V$.
%   Therefore, for all $k$, the chain group $C_k(X\setminus B) = C_k(U)\oplus C_k(V)$ and the relative chain group
%   \[ C_k(X\setminus B, V) = C_k(X\setminus B) / C_k(V) = (C_k(U)\oplus C_k(V)) / C_k(V)\cong C_k(U) \]
%   as $C_k(U) / C_k(V) \cong C_k(U)$.
%
%   It follows that for boundary maps $\partial^{k} : C_k(X\setminus B, V)\to C_{k-1}(X\setminus B, V)$ and $\partial_U^{k} : C_k(U)\to C_{k-1}(U)$ we have $\ker~\partial^k\cong \ker~\partial_U^k$ and $\im~\partial^{k+1}\cong \im~\partial_U^{k+1}$ thus
%   \[ \hom_k(X\setminus B, V) = \ker~\partial^k / \im~\partial^{k+1} \cong \ker~\partial_U^k / \im~\partial_U^{k+1} = \hom_k(U).\]
% \end{proof}
% \begin{proof}
%   By Lemma~\ref{lem:oplus_separates} $\hom_k(X\setminus B) \cong \hom_k(U)\oplus \hom_k(V)$ therefore we may write the long exact sequence of the pair $(X\setminus B, V)$ as
%   \[ \ldots\xrightarrow{i^k_*} \hom_k(U)\oplus \hom_k(V)\xrightarrow{j^k_*} \hom_k(X\setminus B, V)\xrightarrow{\partial^k_*}\hom_{k-1}(V)\to\ldots. \]
%   For any $[y]\in \hom_k(X\setminus B, V)$ with representative $y\in C_k(X\setminus B) / C_k(V)$ clearly $y\notin C_k(V)$ so $[y]\in \ker~\partial_*^k$ which implies $[y]\in\im~j_*^k$ by exactness.
%   So there exists some $[x]\in\hom_k(U)\oplus \hom_k(V)$ that does not have a preimage in $\hom_k(V)$ so $[x]\in\hom_k(U)$.\textbf{TODO unique}.
%
%   On the other hand, suppose $[x]\in\hom_k(U)$.
%   Clearly $[x]$ does not have a preimage in $\hom_k(V)$ as $U$, $V$ are not path connected.
%   So there must exist some nonzero $[y]\in\hom_k(X\setminus B, V)$ such that $j_*^k[x] = [y]$ for all such $[x]$.
%
%   \textbf{TODO bijection}
% \end{proof}
% \begin{proof}
%     If $B$ separates $X$ with the pair $(U, V)$ then $U,V$ are not path connected and $X\setminus B = U\cup V$.
%
%     Suppose $[y]\in \hom_k(X\setminus B, V)$ is nonzero.
%     Let $j_*^k : \hom_k(X\setminus B)\to \hom_k(X\setminus B, V)$ and $\partial_*^k : \hom_k(X\setminus B, V) \to \hom_{k-1}(B)$ be maps in the long exact sequence of the pair $(X\setminus B, V)$.
%     Choose a representative chain $y \in C_k(X\setminus B, V) = C_k(X\setminus B) / C_k(V)$.
%     So $y\in C_k(X\setminus B)$ and $y\notin C_{k-1}(V)$.
%     Because $X\setminus B = U\cup V$ and $U,V$ are not path connected $\partial^k(y)\notin C_{k-1}(V)$ thus $[y]\in\ker~\partial^k_*$.
%     Therefore, by exactness, $[y]\in \im~j^k_*$ and any representative of $[x]\in\hom_k(X\setminus B)$ such that $j^k_*[x] = [y]$ must be in $C_k((X\setminus B)\setminus U) = C_k(U)$.
%
%     On the other hand, if $[y]\in \hom_k(U)$ then $[y]\in \hom_k(\overline{B}, V)$ as $U\subset\overline{B}$ and, because $U,V$ are not path connected, $[y]$ is trivial in $\hom_k(\overline{B}, V)$ if and only if $[x]$ is trivial in $\hom_k(U)$.
% \end{proof}

% \begin{corollary}\label{cor:iso_surrounds}
%   If $B\subset D$ surrounds $D\subset S^d$ then for all $k$
%   \[ \hom_k(D\setminus B)\cong \hom_k(S^d\setminus B, S^d\setminus D).\]
% \end{corollary}

\begin{equation}\begin{tikzcd}
    \hom_k(B_0)\arrow{r}{i^k_0}\arrow{d}{a^k} &
    \hom_k(D_0)\arrow{r}{j^k_0} \arrow{d}{b^k} &
    \hom_k(D_0, B_0)\arrow{r}{\partial^k_0}\arrow{d}{d^k} &
    \hom_{k-1}(B_0)\arrow{r}{i^{k-1}_0}\arrow{d}{a^{k-1}} &
    \hom_{k-1}(D_0) \arrow{d}{b^{k-1}}\\
    %
    % \hom_k(Q^\delta)\arrow{r}{p^k_\delta} \arrow{d}{\psi^k_\delta} &
    % \hom_k(P^\delta)\arrow{r}{q^k_\delta} \arrow{d}{\rho^k_\delta} &
    % \hom_k(P^\delta, Q^\delta)\arrow{r}{\partial^k_\delta} \arrow{d}{\nu^k_\delta} &
    % \hom_{k-1}(Q^\delta)\arrow{r}{p^{k-1}_\delta} \arrow{d}{\psi^{k-1}_\delta} &
    % \hom_{k-1}(P^\delta)\arrow{d}{\rho^{k-1}_\delta}\\
    % %
    \hom_k(\B)\arrow{r}{i_*^k}\arrow{d}{f^k} &
    \hom_k(\D)\arrow{r}{j_*^k}\arrow{d}{g^k} &
    \hom_k(\D, \B)\arrow{r}{\partial_*^k}\arrow{d}{h^k} &
    \hom_{k-1}(\B)\arrow{r}{i_*^{k-1}}\arrow{d}{f^{k-1}} &
    \hom_{k-1}(\D)\arrow{d}{g^{k-1}}\\
    %
    % \hom_k(Q^\gamma)\arrow{r}{p^k_\gamma} \arrow{d}{\psi^k_\gamma} &
    % \hom_k(P^\gamma)\arrow{r}{q^k_\gamma} \arrow{d}{\rho^k_\gamma} &
    % \hom_k(P^\gamma, Q^\gamma)\arrow{r}{\partial^k_\gamma} \arrow{d}{\nu^k_\gamma} &
    % \hom_{k-1}(Q^\gamma)\arrow{r}{p^{k-1}_\gamma} \arrow{d}{\psi^{k-1}_\gamma} &
    % \hom_{k-1}(P^\gamma)\arrow{d}{\rho^{k-1}_\gamma}\\
    % %
    \hom_k(B_1)\arrow{r}{i^k_1} &
    \hom_k(D_1)\arrow{r}{j^k_1} &
    \hom_k(D_1, B_1)\arrow{r}{\partial^k_1} &
    \hom_{k-1}(B_1)\arrow{r}{i^{k-1}_1} &
    \hom_{k-1}(D_1)
\end{tikzcd}\end{equation}

% \begin{equation}\begin{tikzcd}
%     \hom_k(B_0)\arrow{r}{i^k_0}\arrow{d}{\phi^k_0} &
%     \hom_k(D_0)\arrow{r}{j^k_0} & %\arrow{d}{\eta^k_0} &
%     \hom_k(D_0, B_0)\arrow{r}{\partial^k_0}\arrow{d}{\mu^k_0} &
%     \hom_{k-1}(B_0)\arrow{r}{i^{k-1}_0}\arrow{d}{\phi^{k-1}_0} &
%     \hom_{k-1}(D_0) \\% \arrow{d}{\eta^{k-1}_0}\\
%     %
%     \hom_k(Q^\delta)\arrow{r}{p^k_\delta} \arrow{d}{\psi^k_\delta} &
%     \hom_k(P^\delta)\arrow{r}{q^k_\delta} \arrow{d}{\rho^k_\delta} &
%     \hom_k(P^\delta, Q^\delta)\arrow{r}{\partial^k_\delta} \arrow{d}{\nu^k_\delta} &
%     \hom_{k-1}(Q^\delta)\arrow{r}{p^{k-1}_\delta} \arrow{d}{\psi^{k-1}_\delta} &
%     \hom_{k-1}(P^\delta)\arrow{d}{\rho^{k-1}_\delta}\\
%     %
%     \hom_k(B_0^{2\delta})\arrow{r}{i^k}\arrow{d}{\phi^k} &
%     \hom_k(D_0^{2\delta})\arrow{r}{j^k}\arrow{d}{\sigma^k} &
%     \hom_k(D_0^{2\delta}, B_0^{2\delta})\arrow{r}{\partial^k}\arrow{d}{\mu^k} &
%     \hom_{k-1}(B_0^{2\delta})\arrow{r}{i^{k-1}}\arrow{d}{\phi^{k-1}} &
%     \hom_{k-1}(D_0^{2\delta})\arrow{d}{\sigma^{k-1}}\\
%     %
%     \hom_k(Q^\gamma)\arrow{r}{p^k_\gamma} \arrow{d}{\psi^k_\gamma} &
%     \hom_k(P^\gamma)\arrow{r}{q^k_\gamma} \arrow{d}{\rho^k_\gamma} &
%     \hom_k(P^\gamma, Q^\gamma)\arrow{r}{\partial^k_\gamma} \arrow{d}{\nu^k_\gamma} &
%     \hom_{k-1}(Q^\gamma)\arrow{r}{p^{k-1}_\gamma} \arrow{d}{\psi^{k-1}_\gamma} &
%     \hom_{k-1}(P^\gamma)\arrow{d}{\rho^{k-1}_\gamma}\\
%     %
%     \hom_k(B_1)\arrow{r}{i^k_1} &
%     \hom_k(D_1)\arrow{r}{j^k_1} &
%     \hom_k(D_1, B_1)\arrow{r}{\partial^k_1} &
%     \hom_{k-1}(B_1)\arrow{r}{i^{k-1}_1} &
%     \hom_{k-1}(D_1)
% \end{tikzcd}\end{equation}
